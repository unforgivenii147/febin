    cleaned = []
    for line in lines:
        if line.startswith("#!"):
            cleaned.append(line)
            continue
        if "#" in line and not line.strip().startswith("#"):
            indx = line.index("#")
            cleaned.append(line[:indx] + "\n")
            continue
        if not line.strip().startswith("#"):
            cleaned.append(line)
